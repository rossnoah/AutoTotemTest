name: AutoTotemTest
version: '${project.version}'
main: net.vanillaplus.autototemtest.Main
api-version: 1.16
commands:
  totemtest:
    description: Used to test for auto totem
    usage: /<command>
    permission: totemtest.use